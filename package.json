{
  "publisher": "venberstep-vanilla",
  "name": "ai-commit-message",
  "displayName": "AI Commit Message Generator for VS Code",
  "description": "VS Code 插件：通过第三方 AI API（OpenAI、Azure、本地 LLM 等兼容接口）自动生成 Git Commit Message。A VS Code extension that generates Git commit messages via third-party AI APIs (OpenAI-compatible endpoints).",
  "version": "1.4.0",
  "author": "venberstep",
  "contributors": [
    "venberstep (original author)",
    "Vanilla_Yukirin <Vanilla_Yukirin@outlook.com>"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/venberstep/AI-Commit-Message-Generator-for-VS-Code"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.100.0"
  },
  "icon": "resources/icons/logo.png",
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "AI Commit Message Generator",
      "properties": {
        "ai-commit-message.apiProvider": {
          "type": "string",
          "default": "openai",
          "enum": [
            "openai",
            "claude",
            "azure",
            "custom"
          ],
          "description": "API Service Provider (openai, claude, azure, or custom endpoint)"
        },
        "ai-commit-message.apiUrl": {
          "type": "string",
          "default": "https://api.openai.com/v1/chat/completions",
          "description": "API Base URL (default varies by provider: OpenAI: https://api.openai.com/v1/chat/completions, Claude: https://api.anthropic.com/v1/messages, Azure: https://{resource-name}.openai.azure.com/openai/deployments/{deployment-id}/chat/completions)"
        },
        "ai-commit-message.model": {
          "type": "string",
          "default": "gpt-3.5-turbo",
          "description": "Model Name (e.g. gpt-3.5-turbo for OpenAI, claude-3-5-sonnet-20241022 for Claude)"
        },
        "ai-commit-message.apiKey": {
          "type": "string",
          "default": "",
          "description": "[Optional] API Key (if not set, you'll be prompted at runtime)"
        },
        "ai-commit-message.promptZH": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "将 git diff 中的所有修改**合并**为**唯一一条**符合 Conventional Commits 规范的提交信息。\n\n核心原则: 无论修改了多少文件，最终输出**只能有一个 Header 和一个 Body**。\n\n要求:\n- Header: <type>(<scope>): <中文描述，50字内，祈使语气，无句号>\n- Scope 选择: 必须将所有修改归纳为一个最主要的 scope，或使用 \"project\"、\"refactor\" 等通用词。**严禁**输出多个 Header。\n- Body: 必须包含，用 - 列表说明，按逻辑功能归类（如\"修复构建问题\"、\"优化 UI\"）。\n- **严禁**按文件分段（如 \"feat(A): ... feat(B): ...\" 是错误的）。\n- 忽略纯格式化变动（除非是 style 类型）\n- 仅输出提交信息，不要代码块或解释",
          "description": "中文提示词模板 (Chinese Prompt Template)"
        },
        "ai-commit-message.promptEN": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "Consolidate ALL changes in the git diff into **A SINGLE** Conventional Commits message.\n\nCORE PRINCIPLE: Regardless of how many files are changed, output **ONLY ONE Header and ONE Body**.\n\nRequirements:\n- Header: <type>(<scope>): <English, max 50 chars, imperative, no period>\n- Scope: Summarize all changes into one primary scope, or use generic ones like \"project\", \"refactor\". **DO NOT** output multiple headers.\n- Body: Required, use - bullets, grouped by logical functionality (e.g., \"Fix build issues\", \"Optimize UI\").\n- **STRICTLY FORBIDDEN** to split by file (e.g., \"feat(A): ... feat(B): ...\" is WRONG).\n- Ignore whitespace-only changes (unless style type)\n- Output ONLY the commit message, no code blocks or explanation",
          "description": "英文提示词模板 (English Prompt Template)"
        }
      }
    },
    "commands": [
      {
        "command": "ai-commit-message.generate",
        "title": "AI: Generate Commit Message",
        "icon": "$(wand)"
      },
      {
        "command": "ai-commit-message.clearApiKey",
        "title": "AI: Clear API Key"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "ai-commit-message.generate",
          "group": "navigation@100",
          "when": "scmProvider == git"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.100.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2"
  }
}